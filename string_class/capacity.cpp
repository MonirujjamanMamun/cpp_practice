#include <bits/stdc++.h>

using namespace std;

int main()
{

    string name;
    cin >> name;
    // int s = name.size();
    // int s = name.capacity();
    // cout << s << endl;
    // bool a = name.empty();
    // cout << a << endl;
    cout << name << endl;
    name.resize(6);
    cout << name << endl;
    return 0;
}